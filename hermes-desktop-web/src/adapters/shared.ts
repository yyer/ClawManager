// Compatibility for existing local tests. The renderer alias resolves directly
// to this complete verified upstream entry, never to a hand-selected facade.
export * from '../../.upstream/source/apps/shared/src/index'

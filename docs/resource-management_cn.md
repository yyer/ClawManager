[← 返回 README](../README.zh-CN.md)

# 资源管理指南

资源管理是面向用户的 OpenClaw 工作空间启动配置中心，用来准备和复用配置内容。它与管理端的**安全防护**是两个独立功能：资源管理负责配置和交付，安全防护负责平台风险观察与治理。

![OpenClaw 资源管理](./main/resource-management-current.png)

## 页面结构

- **资源**：搜索和管理单项资源定义。
- **资源包**：把多项资源组合成可重复选择的启动配置。
- **注入记录**：查看实例创建时编译、并在重启时复用的注入快照。

## 资源

当前页面展示四类资源：

- **通道**：可新建、编辑、启用/停用、克隆和删除通信配置。Telegram、钉钉、企业微信、Slack、飞书等受支持模板提供表单和 JSON 两种编辑方式。凭据应填写在对应配置字段，不要写入公开描述。
- **技能**：可上传一个或多个 ZIP、处理导入冲突、下载或删除已上传技能。目录浏览、版本归属、发布和后续安装由 **Skill Hub** 管理。
- **智能体**：当前仅保留资源类型入口，页面暂不支持配置。
- **定时任务**：可使用简易表单或高级 JSON 创建和编辑 OpenClaw 任务；支持 cron、固定间隔、单次执行，以及公告、Webhook 或不投递等方式。

底层资源模型还包含会话模板和日志策略，但当前页面有意不展示。

## 资源包

资源包把已启用资源和符合条件的已上传技能组合成可重复使用的启动选项。用户可以新建、编辑、启用/停用、克隆和删除资源包。多个实例需要相同基线时可在创建实例时选择资源包；只需要少量内容时可使用手动资源。

## 注入记录

注入记录是 ClawManager 为实例编译内容后保存的只读快照，显示快照 ID、交付模式、资源数量、环境变量数量、状态和创建时间。模式包括不注入、手动选择、资源包和归档恢复；状态包括已编译、已激活和失败。

注入记录用于回答“实际交付了什么”，它不是安全事件，也不能替代实例内的最终核对。

## 与其他功能的关系

- **Skill Hub** 管理技能目录、所有者、标签、版本、发布，以及向兼容的 OpenClaw、Hermes、OpenCode、DeepSeek Harness 实例安装。
- **创建实例**会按 Runtime 支持范围提供归档、资源包、手动资源或技能选择。
- **实例技能管理**用于核对运行工作空间中实际落盘的技能版本。
- **安全防护**是独立的管理端功能，负责 Runtime 防御、隔离、策略、应急响应与审计。Skill Scanner 是安全防护中的一个场景，不属于资源管理页签。

## 相关说明

- [Skill Hub 使用指南](./skill-hub-guide.md)
- [安全防护平台指南](./security-platform_cn.md)
- [安全防护平台指南](./security-platform_cn.md)
- [用户指南](./use_guide_cn.md)
